絞り込み内容
%br

バンド
- if @split_band.present?
  - @split_band.each do |n|
    = @bands.find(n).name
-else
  全バンド
%br

難易度
- if @split_expert.present?
  EXPERT
  - @split_expert.each do |n|
    = n
- else
  指定なし
%br

カテゴリ
- if @split_category.present?
  = @categorys.find(@split_category).name
- else
  すべて
%br
- if @songs.present?
  ランダム1曲
  - song = @songs.sample
  :javascript
    function koshin(){
      location.reload();
    }
  %input{:onclick => "koshin()", :type => "button", :value => "更新"}/

  %table{:border => "1"}
    %tr
      %th 曲名
      %th バンド名
      %th EASY
      %th NORMAL
      %th HARD
      %th EXPERT
      %th SPECIAL
      %th 配信日
      %th カテゴリ
    %tr
      %td #{song.title}
      %td #{song.band.name}
      %td #{song.easy}
      %td #{song.normal}
      %td #{song.hard}
      %td #{song.expert}
      %td #{song.special}
      %td #{song.delivery}
      %td #{song.category.name}
  ＜楽曲一覧＞
  .top__song__list--original
    %table{:border => "1"}
      %tr
        %th 曲名
        %th バンド名
        %th EASY
        %th NORMAL
        %th HARD
        %th EXPERT
        %th SPECIAL
        %th 配信日
        %th カテゴリ
      - @songs.each do |song|
        %tr
          %td #{song.title}
          %td #{song.band.name}
          %td #{song.easy}
          %td #{song.normal}
          %td #{song.hard}
          %td #{song.expert}
          %td #{song.special}
          %td #{song.delivery}
          %td #{song.category.name}
- else
  %table{:border => "1"}
    %tr
      %th 曲名
      %th バンド名
      %th EASY
      %th NORMAL
      %th HARD
      %th EXPERT
      %th SPECIAL
      %th 配信日
      %th カテゴリ
  該当楽曲なし

