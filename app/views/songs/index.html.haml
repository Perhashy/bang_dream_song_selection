.top
  .top__title
    スマホゲーム『バンドリ！ガールズバンドパーティ！』における
    フリーライブの選曲をサポートするアプリです！
  .top__message
    皆さんは、楽曲選びに困ったことはありませんか？
    %br
    次何やろう？と悩み、長々と楽曲一覧をスクロールした経験があるのではないですか？
    %br
    そんなあなたのために条件に合わせて自動で選曲します！

  = link_to "始める！", "#"

  = form_with(url: search_songs_path, local: true, method: :get) do |f|
    バンド
    = f.collection_check_boxes :band, Band.all, :id, :name, include_hidden: false
    %br
    難易度
    EXPERT
    = f.collection_check_boxes :expert, Song.group(:expert), :expert, :expert, include_hidden: false
    %br
    カテゴリ
    = f.collection_select :category, Category.all, :id, :name, prompt:"すべて"
    %br
    = f.submit "絞り込む"

  .top__song
    ＜バンド一覧＞
    .top__song__list
      - @bands.each do |band|
        = "・#{band.name}"
        %br
      ＜楽曲一覧＞
      .top__song__list--original
        %table{:border => "1"}
          %tr
            %th 曲名
            %th バンド名
            %th EASY
            %th NORMAL
            %th HARD
            %th EXPERT
            %th SPECIAL
            %th 配信日
            %th スタイル
          - @original.each do |song|
            %tr
              %td #{song.title}
              %td #{song.band.name}
              %td #{song.easy}
              %td #{song.normal}
              %td #{song.hard}
              %td #{song.expert}
              %td #{song.special}
              %td #{song.delivery}
              %td #{song.category.name}
          %tr
            %th 曲名
            %th バンド名
            %th EASY
            %th NORMAL
            %th HARD
            %th EXPERT
            %th SPECIAL
            %th 配信日
            %th スタイル
          - @cover.each do |song|
            %tr
              %td #{song.title}
              %td #{song.band.name}
              %td #{song.easy}
              %td #{song.normal}
              %td #{song.hard}
              %td #{song.expert}
              %td #{song.special}
              %td #{song.delivery}
              %td #{song.category.name}
