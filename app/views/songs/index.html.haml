.top
  .top__title
    スマホゲーム『バンドリ！ガールズバンドパーティ！』における
    フリーライブの選曲をサポートするアプリです！
  .top__message
    皆さんは、楽曲選びに困ったことはありませんか？
    %br
    次何やろう？と悩み、長々と楽曲一覧をスクロールした経験があるのではないですか？
    %br
    そんなあなたのために条件に合わせて自動で選曲します！

  = link_to "始める！", "#"

  .top__song
    ＜バンド一覧＞
    .top__song__list
      - @bands.each do |band|
        = "・#{band.name}"
        %br
      ＜楽曲一覧＞
      .top__song__list--original
        %table{:border => "1"}
          %tr
            %th 曲名
            %th バンド名
            %th EASY
            %th NORMAL
            %th HARD
            %th EXPERT
            %th SPECIAL
            %th 配信日
            %th スタイル
          - @original.each do |song|
            %tr
              %td #{song.title}
              %td #{song.band.name}
              %td #{song.easy}
              %td #{song.normal}
              %td #{song.hard}
              %td #{song.expert}
              %td #{song.special}
              %td #{song.delivery}
              %td #{song.style.name}
          %tr
            %th 曲名
            %th バンド名
            %th EASY
            %th NORMAL
            %th HARD
            %th EXPERT
            %th SPECIAL
            %th 配信日
            %th スタイル
          - @cover.each do |song|
            %tr
              %td #{song.title}
              %td #{song.band.name}
              %td #{song.easy}
              %td #{song.normal}
              %td #{song.hard}
              %td #{song.expert}
              %td #{song.special}
              %td #{song.delivery}
              %td #{song.style.name}
